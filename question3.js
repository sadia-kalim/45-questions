//name cases: store a person s name in veriable,print persons name in lower case, upper case and title case.
var Person = "sadia";
var lowercase = Person.toLowerCase();
console.log(lowercase);
var uppercase = Person.toUpperCase();
console.log(uppercase);
var titlecase = "Sadia";
console.log(titlecase);
